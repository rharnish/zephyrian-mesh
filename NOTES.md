

## Project Setup

Run the following commands in your terminal to initialize a vanilla JS template and install Cesium:

npm init vite@latest cesium-app -- --template vanilla
cd cesium-app
npm install cesium vite-plugin-cesium --save
npm install
